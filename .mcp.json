{
  "mcpServers": {
    "csx": {
      "args": [
        "mcp"
      ],
      "_comment": "csx resolves from PATH; the installer puts it there. An absolute user path here only ever worked on one machine.",
      "command": "csx"
    }
  }
}
