name: csx_http_mock
description: Testing package:http with MockClient, and the four defaults that surprise people.
publish_to: none
environment:
  sdk: ^3.5.0
dependencies:
  http: 1.6.0
dev_dependencies:
  test: 1.31.2
