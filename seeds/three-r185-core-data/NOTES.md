# three core data APIs

This contract answers four exact `three@0.185.1` coordinates from the public
Wanted board without requiring a browser, canvas, GPU, or WebGL context.

It records two easy-to-miss identity rules: normalized integer attributes are
decoded when read, and a cloned texture shares its `Source` with the original.
