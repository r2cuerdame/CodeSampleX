name: csx_dart_crypto
description: Hash and HMAC with package:crypto, checked against published vectors.
publish_to: none
environment:
  sdk: ^3.5.0
dependencies:
  crypto: 3.0.7
dev_dependencies:
  test: 1.31.2
