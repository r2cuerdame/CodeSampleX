# Second copy of the acronym trap, in its own directory because a directory can
# only be claimed by one loader. This one is used to measure the wreckage a
# failed autoload leaves behind rather than the error itself.
class SOAPClient
  RAN = true
end
