name: csx_args_runner
description: A package:args CommandRunner CLI, its two UsageException sources, and the option rules that are not what their names say.
publish_to: none
environment:
  sdk: ^3.5.0
dependencies:
  args: 2.7.0
dev_dependencies:
  test: 1.31.2
