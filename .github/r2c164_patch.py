# R2C-164 review fixes are already present on the PR branch.
# This helper is intentionally a no-op now; the modified CI only formats and
# verifies the branch, then removes this file and restores ci.yml from main.
