[project]
name = "poetryproj"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi (>=0.116.0,<0.117.0)",
]

[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"
